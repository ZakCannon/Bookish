<%= form_with(model: copy) do |form| %>
  <% if copy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(copy.errors.count, "error") %> prohibited this copy from being saved:</h2>

      <ul>
        <% copy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.label :borrower %>
    <%= form.text_field :borrower, placeholder: "Eg. F. Scott Fitzgerald, leave blank if not taken out" %>
    <br>
    <%= form.label :due_date %>
    <%= form.text_field :due_date, placeholder: "Eg. 26/09/2022, leave blank if not taken out" %>
    <br>
    <%= form.submit %>
  </div>
<% end %>