<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <div class="field">
      <%= form.label :author %>
      <%= form.text_field :author, placeholder: "Eg. F. Scott Fitzgerald" %>
    </div>
    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title, placeholder: "Eg. The Great Gatsby" %>
    </div>
    <div class="field">
      <%= form.label :isbn %>
      <%= form.text_field :isbn, placeholder: "Eg. 9780684801520" %>
    </div>
    <div class="field">
      <%= form.label :genre %>
      <%= form.text_field :genre, placeholder: "Optional" %>
    </div>
    <div class="field">
      <%= form.label :description %>
      <div class="control">
          <%= form.text_area :description, placeholder: "Optional", class:"textarea"%>
      </div>
    </div>
    <div class="control">
      <%= form.submit class:"button is-link"%>
    </div>
  </div>
<% end %>
